{
  "name": "flavor-theme",
  "version": "2.4.0",
  "private": true,
  "description": "Flavor WordPress Theme - M3 Design System",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
